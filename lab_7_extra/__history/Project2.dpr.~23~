program Project2;

uses
  System.SysUtils;

var
  f: TextFile;
  s: String;

begin
  AssignFile(f, 'myText.txt');
  Reset(f);
  while not Eof(f) do
  begin
    readln(f, s);
    writeln(s);
  end;
  CloseFile(f);

  readln;

end.
